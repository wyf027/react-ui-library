# Modal

Modal dialog: portal-mounted overlay, **Escape** and **click-outside** close, `role="dialog"` + `aria-modal`. Implementation splits shell into `parts/ModalDialog`; header chrome is shared via `_internal/DialogHeader`.

- Docs: `docs/components/modal.md`
