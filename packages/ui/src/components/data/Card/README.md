# Card

Container for grouped content with optional **title**, **extra**, **cover**, **actions**, and **loading** (Spin overlay).

- Docs: `docs/components/card.md`
- Uses `Spin` from `feedback` when `loading` is true.
