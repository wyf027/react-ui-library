# Table

Data table with local **search**, **column visibility**, **filters**, **sort**, and **pagination**. Layout class strings live in `tableClassNames.ts`; column types in `Table.types.ts`.

- Docs: `docs/components/table.md`
