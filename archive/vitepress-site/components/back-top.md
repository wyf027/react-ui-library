# BackTop 回到顶部

返回页面顶部的按钮。

## 示例

<LivePlayground :code="`
() => {
  return (
    <BackTop visibilityHeight={0} />
  )
}
`" />

## API

| 属性 | 说明 | 类型 | 默认值 |
| --- | --- | --- | --- |
| visibilityHeight | 滚动高度达到此参数值才出现 | `number` | `400` |
